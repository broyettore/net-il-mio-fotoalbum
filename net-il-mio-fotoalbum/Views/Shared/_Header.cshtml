<header>
    <div class="container d-flex justify-content-between align-items-center">
        <h1 class="fs-4 fw-bold">
            <a class="custom-link" href="@Url.Action("UserIndex", "Photo")">Loner's Diary'</a>
        </h1>
        <nav>
            <ul class="d-flex align-items-center py-2 my-2">

                @if (User.IsInRole("ADMIN"))
                {
                    <li class="m-1 p-1">
                        <a class="custom-link" href="@Url.Action("Index", "Photo")">Photos</a>
                    </li>
                }

                @if (User.IsInRole("ADMIN"))
                {
                    <li class="m-1 p-1">
                        <a class="custom-link" href="@Url.Action("Index", "Category")">Categories</a>
                    </li>
                }

                <partial name="_LoginPartial" />
            </ul>
        </nav>
    </div>
</header>
